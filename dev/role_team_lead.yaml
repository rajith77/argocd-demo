apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: default
  name: team-lead
rules:
- apiGroups: [""] # "" indicates the core API group
  resources: ["pods", "deployment", "service", "route"]
  verbs: ["get", "watch", "list"]
